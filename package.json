{
  "name": "manuel-donini-cv",
  "license": "MIT",
  "devDependencies": {
    "cross-env": "^7.0.3",
    "puppeteer": "^21.9.0",
    "tailwindcss": "^3.0"
  },
  "scripts": {
    "prod": "cross-env NODE_ENV=production npx tailwindcss -i src/css/main.css -o dist/css/main.css --minify",
    "dev": "npx tailwindcss -i src/css/main.css -o dist/css/main.css",
    "watch": "npx tailwindcss -i src/css/main.css -o dist/css/main.css --watch",
    "pdf": "node generate-pdf.js"
  }
}
